<Window x:Class="portfolio.MainWindow"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:portfolio.ViewModels"
        xmlns:inf="clr-namespace:portfolio.Infrastructure"
        mc:Ignorable="d"
        Title="Crypto portfolio" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <inf:ImageSourceConverter x:Key="ImageSourceConverter"/>
        <Style TargetType="DataGrid">
            <Setter Property="RowBackground" Value="Beige"></Setter>
            <Setter Property="Background" Value="LightGoldenrodYellow"/>
            <Setter Property="Foreground" Value="DimGray"/>
            <Setter Property="BorderBrush" Value="LightSteelBlue"></Setter>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="DataGridCell" >
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="MediumPurple"/>
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Brevia"/>
            <Setter Property="Background" Value="BlanchedAlmond"/>
            <Setter Property="Opacity" Value="0.78"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Brevia Light"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Brevia"/>
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Resources/pastelAbstraction.jpg" Opacity="0.8"></ImageBrush>
    </Window.Background>
    <Grid x:Name="gridGeneral" Margin="8. 8. 8. 8.">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.55*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="1.1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid x:Name="gridData" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="172*"/>
                <ColumnDefinition Width="305*"/>
            </Grid.ColumnDefinitions>
            <TabControl Margin="5,0,0,5" BorderBrush="Gainsboro" Grid.ColumnSpan="2">
                <TabControl.Background>
                    <ImageBrush ImageSource="pack://application:,,,/Resources/light-yellow.jpg" Opacity="0.75"></ImageBrush>
                </TabControl.Background>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="0,0,0,0" BorderBrush="LemonChiffon" CornerRadius="4,4,0,0" Margin="2,0">
                                        <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="LemonChiffon" />
                                            <Setter TargetName="Border" Property="Opacity" Value="0.8" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="DarkKhaki" />
                                            <Setter TargetName="Border" Property="Opacity" Value="0.6" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="Coins">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="105*"/>
                            <ColumnDefinition Width="361*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition Height="80"></RowDefinition>
                        </Grid.RowDefinitions>


                        <Border Grid.Row="0" Grid.ColumnSpan="2">
                            <DockPanel >

                                <!--<GroupBox  Width="448" DockPanel.Dock="Left">-->
                                <!--Header="Coin list"-->
                                <DataGrid
                                    Margin="5,5,5,5" x:Name="grCoinsData" Grid.Row="0" Grid.Column="1" 
                                          ItemsSource="{Binding Coins}"
                                          AutoGenerateColumns = "False"
                                    SelectedValue="{Binding SelectedCoin}" SelectedIndex="0">
                                    <!--SelectionChanged="transactionsOnCoinToList">-->

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction
                                        Command="{Binding GetTransactionCommand}"
                                        CommandParameter="{Binding SelectedCoin.CoinId}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn>
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Image 
                                                        Stretch="Fill" 
                                                        Width="20" Height="20" 
                                                        Source="{Binding PictureName, Converter={StaticResource ImageSourceConverter}, Mode=TwoWay}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Coin" Width="35"
                                                            Binding="{Binding Path=ShortName}"></DataGridTextColumn>
                                        <DataGridTextColumn Header="Amount"
                                                            Binding="{Binding Path=Amount}"></DataGridTextColumn>
                                        <DataGridTextColumn Header="Purchase price"
                                                            Binding="{Binding Path=AveragePurchasePrice, StringFormat={}{0:C}}"></DataGridTextColumn>
                                        <DataGridTextColumn Header="Current price"
                                                            Binding="{Binding Path=CurrentPrice, StringFormat={}{0:C}}"></DataGridTextColumn>
                                        <DataGridTextColumn Width="75" Header="Sum"
                                                            Binding="{Binding Path=Sum, StringFormat={}{0:C}}"></DataGridTextColumn>
                                        <DataGridTextColumn Width="75" Header="Diff"
                                                            Binding="{Binding Path=Diff, StringFormat={}{0:C}}"></DataGridTextColumn>
                                    </DataGrid.Columns>

                                </DataGrid>

                                <!--<ListView x:Name="Coins"
                                          ItemsSource="{Binding Coins}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Image Width="20" 
                                                       Source="{Binding PictureName, Converter={StaticResource ImageSourceConverter}}"/>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                    <TextBlock Text="{Binding ShortName}" HorizontalAlignment="Left" Margin="30,0,0,0"/>
                                                    <TextBlock Text="{Binding Amount}" HorizontalAlignment="Left" Margin="30,0,0,0"/>
                                                    <TextBlock Text="{Binding AveragePurchasePrice, StringFormat={}{0:0.00 $}}" Margin="5,0,0,0"/>
                                                    <TextBlock Text="{Binding CurrentPrice, StringFormat={}{0:0.00 $}}" Margin="5,0,0,0"/>
                                                    <TextBlock Text="{Binding DateUpdate, StringFormat={}{0:dd.MM.yyyy г.р.}}" HorizontalAlignment="Left" Margin="5,0,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>-->
                                <!--</GroupBox>-->
                            </DockPanel>
                            <!--<ListView x:Name="Coins" 
                          ItemsSource="{Binding Coins}"
                          SelectedValue="{Binding SelectedCoin}" SelectedIndex="0">
                            -->
                            <!--#region Обработка события Selection Canged -->
                            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction
                                        Command="{Binding GetTransactionCommand}"
                                        CommandParameter="{Binding SelectedCoin.CoinId}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            -->
                            <!--#endregion-->
                            <!--
                            <ListView.Background>
                                <ImageBrush ImageSource="pack://application:,,,/Resources/light-yellow.jpg" Opacity="0.65"></ImageBrush>
                            </ListView.Background>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CoinName}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>-->
                        </Border>
                        <DockPanel Grid.ColumnSpan="2" Margin="0,305,0,0" Grid.RowSpan="2">
                            <GroupBox Header="Coin transactions" Margin="0" Padding="0">
                                <ListView x:Name="lvTransactions" ItemsSource="{Binding Transactions}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Symbol}"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </GroupBox>
                        </DockPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="Transaction">
                    <Border>
                        <DockPanel Grid.Row="1">
                            <DataGrid
                                    Margin="5,5,5,5" x:Name="grTransactionData" Grid.Row="0" Grid.Column="1" 
                                          ItemsSource="{Binding AllTransactions}"
                                          AutoGenerateColumns = "False">
                                <!--SelectedValue="{Binding SelectedTransaction}" SelectedIndex="0">-->
                                <!--<i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction
                                        Command="{Binding GetCoinCommand}"
                                        CommandParameter="{Binding SelectedTransaction.TransactionId}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>-->

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Transaction" Width="95"
                                                            Binding="{Binding Path=Symbol}"></DataGridTextColumn>
                                    <DataGridTextColumn Header="Amount"
                                                            Binding="{Binding Path=Amount}"></DataGridTextColumn>
                                    <DataGridTextColumn Header="Price"
                                                            Binding="{Binding Path=Priсe, StringFormat={}{0:C}}"></DataGridTextColumn>
                                    <DataGridTextColumn Width="75" Header="Sum"
                                                            Binding="{Binding Path=Sum, StringFormat={}{0:C}}"></DataGridTextColumn>
                                    <DataGridTextColumn Header="side" Width="35"
                                                            Binding="{Binding Path=Side}"></DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                        <!--<ListView x:Name="Coins" 
                          ItemsSource="{Binding Coins}"
                          SelectedValue="{Binding SelectedCoin}" SelectedIndex="0">
                            -->
                        <!--#region Обработка события Selection Canged -->
                        <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction
                                        Command="{Binding GetTransactionCommand}"
                                        CommandParameter="{Binding SelectedCoin.CoinId}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            -->
                        <!--#endregion-->
                        <!--
                            
                            <ListView.Background>
                                <ImageBrush ImageSource="pack://application:,,,/Resources/light-yellow.jpg" Opacity="0.65"></ImageBrush>
                            </ListView.Background>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CoinName}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>-->
                    </Border>
                </TabItem>
            </TabControl>
        </Grid>

        <Grid x:Name="gridStatistic" Grid.Column="1" Grid.Row="0">
            <TabControl Margin="10" BorderBrush="Gainsboro">
                <TabControl.Background>
                    <ImageBrush ImageSource="pack://application:,,,/Resources/light-yellow.jpg" Opacity="0.75"></ImageBrush>
                </TabControl.Background>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="0,0,0,0" BorderBrush="LemonChiffon" CornerRadius="4,4,0,0" Margin="2,0">
                                        <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="LemonChiffon" />
                                            <Setter TargetName="Border" Property="Opacity" Value="0.8" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="DarkKhaki" />
                                            <Setter TargetName="Border" Property="Opacity" Value="0.6" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="Desc">
                    <Grid >
                        <!--DataContext="{Binding SelectedCoin}"-->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.4*"></RowDefinition>
                            <RowDefinition Height="0.4*" ></RowDefinition>
                            <RowDefinition Height="3*" ></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"></ColumnDefinition>
                            <ColumnDefinition Width="33*"/>
                            <ColumnDefinition Width="35*"></ColumnDefinition>
                            <ColumnDefinition Width="35*"></ColumnDefinition>
                            <ColumnDefinition Width="35*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                       
                        <GroupBox Grid.Row="2" Grid.ColumnSpan="3" Header="Last buy transaction" Margin="2,2,2,2" Padding="2">
                            <ListView x:Name="lvTransaction" >
                                <!--ItemsSource="{Binding Buyings}"-->
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition></RowDefinition>
                                                <RowDefinition></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition></ColumnDefinition>
                                                <ColumnDefinition></ColumnDefinition>
                                                <ColumnDefinition></ColumnDefinition>
                                            </Grid.ColumnDefinitions>
                                            <!--<TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding TransactionId}"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Amount}"/>
                                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Priсe}"/>-->
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </GroupBox>

                      
                    </Grid>

                </TabItem>


                <TabItem Header="Second">
                    <TextBlock>test2</TextBlock>
                </TabItem>
            </TabControl>

            <!--<Border>
                    <ListBox x:Name="listStatistic" Margin="15, 5, 5, 5" Height="Auto">
                        <ListBox.Background>
                            <ImageBrush ImageSource="pack://application:,,,/Resources/light-yellow.jpg" Opacity="0.75"></ImageBrush>
                        </ListBox.Background>
                    </ListBox>
           </Border>-->
        </Grid>


        <Grid x:Name="gridPanel" Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="st1" Grid.Column="0" Margin="0 100 15 10" VerticalAlignment="Bottom">
                <Button Height="26" Margin="0,1,15,1" Background="LightGreen" Name="btnInsert" Content="сurrent"/>
                <Button Height="26" Margin="0,1,15,1" Background="Moccasin" Name="btnFind" Content="by bought price"/>
                <Button Height="26" Margin="0,1,15,1" Background="LightSalmon" Name="btnUpdate" Content="ratio"/>
            </StackPanel>
            <StackPanel x:Name="st2" Grid.Column="1" Margin="30,15,10,80">
                <Button x:Name="AddCoin" Content="Add coin" Command="{Binding NewCoinCommand}"/>
                <Button x:Name="AddTransaction" Content="Add transaction"/>
                <Button Visibility="Hidden" x:Name="Find" Content="Find"/>

                <!--<Button Content="Simple Command" Command="{Binding SimpleCommand}" />-->
                <!--<Button Name="Update"  Content="Update"/>-->
            </StackPanel>
        </Grid>
    </Grid>
</Window>
